# ─── Shopify ─────────────────────────────────────────────────────────────────
# Your Shopify store URL (without https://)
# e.g. my-store.myshopify.com
SHOPIFY_STORE_URL=your-store.myshopify.com

# Partner Dashboard app credentials (used only by: npm run setup-token)
# Found in: Partner Dashboard → Apps → your app → App setup → Client credentials
SHOPIFY_CLIENT_ID=your-client-id
SHOPIFY_CLIENT_SECRET=your-client-secret

# Permanent offline access token — populated automatically by: npm run setup-token
# Do NOT fill this in manually; run the setup script to generate it.
SHOPIFY_ACCESS_TOKEN=

# ─── Resend ──────────────────────────────────────────────────────────────────
# API key from https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ─── Email ───────────────────────────────────────────────────────────────────
# Sender address — must be a verified domain in your Resend account
EMAIL_FROM=reports@yourdomain.com

# Recipient — Amber's email address
EMAIL_TO=amber@yourdomain.com
